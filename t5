#include <stdio.h>
#include <stdlib.h>

int main(){
	FILE *fp;
	int i, j;

	fp = fopen("arquivo.txt","w");

	for(i=0;i<10;i++)
		for(j=0;j<20;j++){
			fprintf(fp,"%d\n",(i*(256)+j));
		}
	fclose(fp);

	return 0;
}
